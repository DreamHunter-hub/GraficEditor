using System;

namespace GraficEditor.Enums.Transformation {
    /// <summary>
    /// Перечисление для определения типов преобразования изображений в RGB-формат.
    /// На данный момент не содержит элементов, ожидается добавление в будущем.
    /// </summary>
    public enum RGBTransformationType {
        // В будущем сюда могут быть добавлены типы преобразования в RGB.
    }
}
